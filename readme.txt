#To Build
#Download project and go into root folder of the project in command prompt then run

mvn clean install

#To Run, in command prompt go into target folder and run
java -jar  ./transformerscoring 0.0.1-SNAPSHOT

#The swagger ui will be accessible at http://localhost:8080/swagger-ui.html